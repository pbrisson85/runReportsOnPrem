const calcInventoryAvailable = finalData => {
  // Available inventory is on hand plus POs minus SOs
  console.log('calcInventoryAvailable.js: finalData = ', finalData)

  return finalData
}

module.exports = calcInventoryAvailable
