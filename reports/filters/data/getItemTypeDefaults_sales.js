// item types are pulled from db but defaults are manually added here.

const getItemTypeDefaults = () => {
  const defaults = ['SEAFOOD_FG', 'SEAFOOD_SECONDS']

  return defaults
}

module.exports = getItemTypeDefaults
